{
    "require-dev": {
        "php-parallel-lint/php-parallel-lint": "^1.3"
    },
    "scripts": {
        "lint": "parallel-lint --exclude vendor --ignore-fails .",
            "test": [
                "composer lint"
             ]
    }
}
